Copy $(pwd)/.rpmmacros into ~/

source repos assumed to be in ~/repositories/
dist-git repos assumed to be in ~/repositories/pkgs

To run the script:
# bash builder.sh -t $BUILD_TYPE -p $PACKAGE -u $GITHUB_USER -r $GITHUB_USER_REPO_NAME -b $GITHUB_USER_REPO_BRANCH -k

BUILD_TYPE: 'tagged' or 'scratch'

PACKAGE: rpm name

GITHUB_USER: PR author/upstream github user id

GITHUB_USER_REPO_NAME: repo/fork name

GITHUB_USER_REPO_BRANCH: repo/fork branch name

KOJI_BRANCH: koji tag branch (NOT the dist-git branch name)
In the case of rawhide, KOJI_BRANCH will be "rawhide" and not "master"
